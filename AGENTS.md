## 多智能体协作规则

### 总体原则

主智能体对整个任务的最终结果负责。主智能体应先分析任务，再决定是否需要委派给子智能体。

优先使用“主智能体 + 子智能体”协作模式处理以下任务：

- 可以拆分为多个独立子任务；
- 涉及多个模块、层次或技术领域；
- 需要并行调查、实现、测试或审查；
- 需要一个独立视角进行代码审查或风险检查。

对于简单、单文件、低风险、无需并行的任务，主智能体可以直接完成，不必强行创建子任务。

### 主智能体职责

主智能体必须：

1. 理解用户目标、项目约束和验收标准。
2. 判断任务是否适合拆分，并设计子任务边界。
3. 为每个子智能体提供明确的背景、目标、允许修改的范围和完成标准。
4. 避免安排互相修改同一文件的并行子任务，除非已明确协调方式。
5. 汇总并审查子智能体的结果、代码变更、测试证据和风险。
6. 解决子任务之间的冲突，必要时要求子智能体返工。
7. 负责最终集成、测试、文档更新和用户交付说明。
8. 不得仅依据子智能体的口头结论判断任务完成，必须检查实际文件和验证结果。

### 子智能体职责

子智能体必须：

1. 只处理主智能体明确分配的任务。
2. 不擅自扩大需求、改变整体架构或修改无关文件。
3. 在开始前确认相关上下文、输入、输出和验收标准。
4. 完成任务后报告：
   - 完成内容；
   - 修改或新增的文件；
   - 执行过的命令；
   - 测试、构建或检查结果；
   - 未解决的问题和潜在风险。
5. 如果发现任务范围不清、依赖缺失或可能影响其他模块，先向主智能体报告。
6. 不得将未验证的结果声明为已完成。
7. 不得执行超出项目授权范围的破坏性操作。

### 任务拆分规则

- 独立且不修改相同文件的任务可以并行执行。
- 存在数据、接口或设计依赖的任务必须按顺序执行。
- 调查、实现、测试和审查可以分别委派给不同子智能体。
- 每个子任务应尽量单一、具体，并包含明确的完成标准。
- 子任务之间应通过文件、接口、报告或测试结果传递信息。

### 审核与验收

主智能体在交付前必须：

- 检查所有子智能体的实际变更；
- 确认没有超出任务范围的修改；
- 运行项目要求的测试、构建、类型检查或 lint；
- 处理测试失败和子任务冲突；
- 明确说明哪些内容已验证，哪些内容仍存在不确定性。

最终交付由主智能体负责，子智能体不得直接替代主智能体向用户作最终结论。