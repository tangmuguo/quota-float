# 测试矩阵

| 平台/范围 | 场景 | 预期 | 自动化 |
| --- | --- | --- | --- |
| 共享呈现 | 正常、未登录、过期、不可用 | 显示正确状态；错误态支持手动刷新 | Vitest |
| 额度解析 | 主/次窗口对调、单窗口、数组、显式百分比与比例 | 按时长分别匹配 5 小时/一周窗口，缺失时不误配 | Rust |
| 额度切换 | 偏好事件、完整/紧凑、中文/英文、所选窗口缺失 | 百分比、颜色、文案和重置时间随周期切换，缺失明确提示 | Vitest |
| 额度偏好 | 旧配置、重启、重复选择、保存失败 | 旧配置默认一周，重启保留选择，失败保持原值 | Rust |
| 双窗口刷新 | 任一周期临近重置、额度下降、断网、退出登录 | 快速刷新和消耗状态覆盖两个窗口，保留合理缓存，退出登录清除旧数据 | Vitest |
| HTTP/安全边界 | 401/403/429、断网、读取/响应上限、敏感日志 | 返回通用安全错误且不泄露敏感数据 | 代码审查；待补自动化/实机 |
| 共享偏好 | 旧配置包含 `locked: true` | 忽略退役字段，后续保存不再写出，窗口不恢复鼠标穿透 | Rust；交互待实机 |
| Windows | ChatGPT/Codex 宿主移动、缩放、最小化 | 组件继续跟随宿主 | Windows CI 构建；待实机 |
| Windows | MSI/NSIS | 两种安装包均生成 | Windows CI |
| macOS | universal app/DMG | Apple Silicon/Intel 通用产物生成 | macOS CI |
| Ubuntu GNOME | 多个不同大小 ChatGPT 窗口 | 跟随当前聚焦窗口，而非最大窗口 | 纯逻辑测试；待实机 |
| Ubuntu GNOME | 多个身份字段含 `codex` | 正确识别 ChatGPT 与 Quota Float 身份 | Vitest |
| Ubuntu 面板 | 320/100 切换和手动拖边 | 只允许两档尺寸，偏好与 React 模式一致 | Rust 配置测试；待实机 |
| Ubuntu 托盘 | 关闭窗口后首次点击显示 | 勾选同步，第一次点击即恢复 | 代码路径；待实机 |
| Ubuntu 托盘 | 5 小时/一周切换、重复点击、重启、切换语言 | 仅当前周期勾选，两个视图立即更新，选择持久保存 | 单元测试与代码审查；待实机 |
| Ubuntu 扩展 | 启动、正常退出、强制结束、手动禁用、卸载 | 正常流程尝试启停；异常退出可手动清理；卸载移除文件 | 包内容检查；待实机 |
| Ubuntu 构建 | `.deb` | 依赖与 GNOME 扩展文件正确 | Ubuntu CI |
| 隐私 | `.deb` 解包扫描 | 实际安装树无禁用文件、密钥或本机构建路径 | `verify-ubuntu-deb.mjs` |
| 发布 | 三平台附件 | Windows zip、macOS zip、`.deb`、SHA-256 齐全 | release workflow |

## 发布门槛

- 前端测试、Web build 和 Rust 测试通过；发布前本地格式检查与 Release job 的 Clippy 通过。
- Windows、macOS、Ubuntu 三个平台的 CI job 全部通过；等待维护者批准不算通过。
- Ubuntu 26.04 GNOME Wayland 完成锚定、两档尺寸、托盘恢复、刷新和扩展生命周期实测。
- Windows/macOS 完成真实桌面安装与基本回归。
- `.deb` 版本、依赖、文件、SHA-256 和解包内容扫描通过。
