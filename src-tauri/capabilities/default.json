{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the embedded quota panel",
  "windows": ["widget"],
  "permissions": [
    "core:default",
    "core:window:allow-outer-position",
    "core:window:allow-set-size",
    "core:window:allow-set-always-on-top"
  ]
}
